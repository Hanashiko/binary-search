# Binary Search

A simple Python implementation of the binary search algorithm.

## How it works

The program generates a sorted list of ~1 million random integers and performs a binary search for a random target value, printing each step of the search process.

## Usage

```bash
python main.py
```